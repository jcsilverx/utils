/**
 * @type {import("prettier").Config}
 */
const config = {
  arrowParens: "always",
  tabWidth: 2,
  semi: true,
  singleQuote: false,
  printWidth: 80,
  trailingComma: "all",
};

export default config;
